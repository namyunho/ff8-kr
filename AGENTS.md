# AGENTS.md — 파이널 판타지 VIII PS1 한국어화

이 저장소는 PlayStation용 《파이널 판타지 VIII》(일본판) Disc 1의 한국어 패치를
만든다. **진행 단계와 다음 게이트는 `docs/roadmap.md`가 정본이다.** 현재 상태
요약은 `README.md`, 매체·구조 기준선은 `docs/disc1-baseline.md`, 글꼴과 문자
인코딩은 `docs/font-analysis.md`, 한글 폰트 판정은
`docs/korean-font-feasibility.md`, 번역 경로는 `docs/translation-pipeline.md`,
**밟은 함정과 예방책은 `docs/lessons.md`가 정본이다 — 착수 전에 그 문서의
체크리스트부터 본다.** 반대로 새로 겪은 실수·시행착오는 커밋하기 전에
그 문서에 남긴다(`docs/git-workflow.md`의 병합 전 확인 목록 참고) — 성공만
기록하고 착각을 누락하면 다음 세션이 같은 값을 다시 치른다. 외부 자료 대조는 `docs/external-sources.md`, 도구 운용은
`docs/reverse-engineering-mcp.md`, 브랜치와 커밋 규칙은
`docs/git-workflow.md`를 정본으로 삼는다.

**외부 커뮤니티 자료를 판정 근거로 쓰지 않는다.** 탐색 방향을 좁히는 데만 쓰고,
채택 전에 이 디스크에 대조해 `docs/external-sources.md`에 결과를 남긴다.

## 하드 불변식

1. 원본 BIN/CUE, BIOS, 추출 파일, RAM/VRAM 덤프와 패치된 전체 이미지는 커밋하지
   않는다. 기본 원본 위치는 `roms/`이며 `.gitignore`를 유지한다.
   `fonts/` 아래 한국어 폰트 소스는 원본 매체가 아니므로 예외로 추적한다.
2. 원본은 크기와 CRC32/MD5/SHA-256을 모두 검증한 뒤 읽는다. 알려진 값과 다르면
   분석·빌드를 중단한다.
3. ISO 파일 좌표(2048), raw 2352바이트 sector 좌표, IMG TOC의 절대 LBA,
   PS-X EXE 파일 오프셋, runtime virtual address를 섞지 않는다.
4. Mode 2 sector는 Form 1/2와 복제 subheader를 판정한다. 변경 sector만 올바른
   EDC/ECC 규칙으로 다시 만들며, 변경하지 않은 sector를 정규화하지 않는다.
5. PS-X EXE는 파일 `+0x800`의 payload가 header의 load address에 적재된다.
   이 환산을 IMG 안의 다른 파일에 적용하지 않는다.
6. **IMG TOC는 디스크 절대 LBA를 담는다.** 자산 크기를 바꾸면 뒤 파일이 밀려
   TOC 전체를 다시 써야 한다. 크기를 유지하는 in-place 교체를 기본으로 한다.
7. **TIM은 `blockSize`가 아니라 RECT가 정본이다.** 게임 로더가 `blockSize`를
   읽지 않는다. 폰트 파일에서 실제로 두 값이 어긋난다.
8. MIPS R3000A 훅은 branch/load delay, live register, `$gp`, cache 갱신과
   overlay 수명을 검산한다. 생성한 모든 명령은 armips와 독립 디스어셈블 결과로
   대조한다.
9. 추출·재조립은 무수정 round-trip을 먼저 통과한다. 최종 이미지 변경은 불변
   원본에 대한 예상 쓰기 범위로 모두 설명돼야 한다.
10. 미확정 판정을 확정으로 표현하지 않는다. 정적 분석 결과는 실행 검증과
    연결되기 전까지 **추정**으로 남긴다.

### 실패에서 나온 불변식

아래는 전부 **이 프로젝트에서 실제로 깨뜨려 본 것**이다. 사연은
`docs/lessons.md`에 있다.

11. **0 은 빈 자리의 증거가 아니다.** 자리를 쓰려면 세 관문을 다 통과한다 —
    ① 파일에서 0 ② **실행 중에도 0**(여러 장면에서) ③ **아무도 가리키지 않음**
    (이미지 전체에서 `j`/`jal`/분기 목적지와 데이터 참조를 훑는다). 셋을 다
    통과해도 미심쩍으면 **칸 수를 줄여서라도 소유가 증명된 자리를 쓴다.**
    다섯 번 밟았다: 목차 간격 / EXE 꼬리 / BSS / VRAM 의 남의 CLUT /
    `jal` 로 불리는 빈 함수.
12. **원본 자료구조를 다시 짜기 전에 오프셋을 전수로 재서 불변식을 찾는다.**
    정렬·패딩·경계는 크기와 내용이 맞아도 깨질 수 있고, **에뮬레이터는 그걸
    통째로 가려 준다.** 실측된 것: MSD 섹션 오프셋 3,648개 전부 4의 배수,
    메뉴 그룹 오프셋 16개 전부 4의 배수(머리도 34가 아니라 36까지 채운다).
13. **에뮬레이터 통과는 검증의 절반이다. 실기는 별도 관문이다.** 실기에서만
    갈리는 것: 자료구조 정렬, R3000 적재 지연 슬롯, 분기 지연 슬롯, 타이밍,
    섹터의 동기·헤더·서브헤더(EDC/ECC 는 오프셋 16부터 계산하므로 앞을 못
    잡는다). 막히면 **원본이 그 환경에서 도는지부터** 확인해 "우리 패치 탓"과
    "환경 탓"을 가른다.
14. **검사는 세지 말고 실제로 해 본다.** 대리 지표(음절 수, 상수 개수)는
    무엇을 안 보고 있는지 못 알려 준다. 인코딩 가능 여부는 **삽입기가 쓰는 그
    인코더를 불러** 확인한다.
15. **상수를 옮기기 전에 셋을 묻는다** — 혼자인가 **짝인가**(짝수/홀수 팔레트는
    `+0x40` 으로 붙어 있다), 부트 EXE 안에만 있는가 **오버레이에도 있는가**,
    찾을 때 값이 아니라 **패턴**으로 고르는가(우연히 같은 바이트가 흔하다).
16. **압축기는 게임 해제기를 옮긴 참조 구현으로만 검증한다.** 우리 인코더를
    우리 디코더로 맞춰 보는 것은 검증이 아니다 — 그렇게 두 버그를 통과시켰다.
17. **사람이 읽은 값은 문맥으로 교차검증한다.** 12x12 픽셀에서 획 하나는 눈으로
    안 갈린다. 실제 대사를 디코드해 말이 되는지 보는 것이 정본이며, 뱅크1
    판독 90자 중 **여덟 개를 문맥이 뒤집었다.**
18. **자원은 "지금 안 쓴다"가 아니라 "우리가 소유한다"로 확보한다.** 누가
    지우는가(동영상·장면 전환)와 **누가 되살리는가**를 따로 묻는다. 되살리는
    경로를 우리가 막고 있으면 그 자원은 잡을 수 없다.
19. **런타임 검증기는 맨 먼저 "지금 도는 게 우리 빌드인가"를 확인한다.** 패치
    표식이 어긋나면 그 사실부터 말하고 멈춘다. 세이브 스테이트는 RAM·VRAM 을
    통째로 복원하므로 **디스크를 바꿔 꽂아도 스테이트 안의 옛 코드가 돈다.**
20. **원인이 안 보이면 추측을 늘리지 말고 가르는 실험을 만든다.** 먼저 원본과
    전체를 대조해 **무엇을 바꿨는지 실측**하고, 실기 왕복이 비싸므로 한 번에
    최대한 많이 가르도록 설계한다.
21. **글자를 그리는 코드는 하나가 아니다.** 메뉴 항목 이름은 텍스처 폰트가
    아니라 EXE 의 벡터 폰트 루틴 `0x8002c540` 이 그린다. 그 루틴은 표
    `0x800528a8` 의 파트 수를 그대로 믿고 **프리미티브 포인터에 경계 검사가
    없다.** 색인 484~511 중 18칸이 쓰레기라 한 글자에 49,312 파트(약 1MB)를
    쏟아 RAM 을 넘기고 BIOS 예외 벡터를 깔아뭉갠다. 그래서 **배치는 미학이
    아니라 안전 문제다** — 금지 색인을 `build_layout_all.py` 의 `VECTOR_BOMB`
    에 못 박아 둔다. 같은 바이트가 자리에 따라 다르게 동작하면 **읽는 코드가
    둘 이상**이라는 신호다.
22. **실기 왕복이 비싸게 느껴지는 순간이 관측 도구를 만들 때다.** 가르는 실험은
    판당 1비트지만 멈춘 순간의 PC 하나는 답 전체를 준다. PCSX-Redux 는 GDB
    스텁(3333)과 웹 API(8080)를 함께 열어 준다 — `scripts/gdb_peek.py` 로 붙는다.
    인터럽트로 붙잡으면 **언제나 예외 벡터가 잡히므로** 게임의 진짜 PC 는 TCB 의
    EPC(`*(*(0x108)) + 8 + 0x80`)에서 읽고, 누가 특정 자리를 망가뜨리는지는
    쓰기 감시점(`--watch`)으로 잡는다.
23. **관측이 유효한 상태에서 뜬 것인지 먼저 검산한다.** 멈춘 상태가 아닌 RAM 을
    보고 **맞는 판단을 취소한** 적이 있다. 「지금 이 화면이 맞다」를 증명하는
    지표를 하나 정해 둔다(예: 메뉴 블록이 RAM 에 올라와 있는가). 판단을 뒤집을
    때는 **뒤집는 근거부터** 본다. 그리고 「없다」는 결론은 **다른 방법으로 한 번
    더** 확인한다 — `/dev/tcp` 가 zsh 에서 안 먹어 열려 있는 포트를 닫혔다고
    단정한 적이 있다.
24. **글리프 배치는 산출물이 아니라 계약이다.** 정본은
    `data/glyph-layout.json` **하나뿐**이고 git 이 추적한다(`version` + `sha256`).
    폰트와 텍스트가 **동시에** 이것을 따라야 하며, 어긋나도 크래시가 아니라
    화면이 조용히 다른 글자가 된다 — 한때 배치 파일이 여섯 개였고 서로 882칸 중
    880칸이 달랐다. 폰트는 새것으로 굽고 `patch_disc.py` 는 옛것을 기본값으로
    읽고 있었다(매번 `--layout` 을 넘겨서 안 터졌을 뿐이다).
    - **재생성하지 않는다.** `build_layout_all.py` 는 `--output` 기본값이
      `work/glyph-layout-proposed.json` 이다. 빈도표에서 다시 만들면 이미
      인코딩한 것이 전부 어긋난다. **디스크 2~4 번역이 들어와도 배치는 그대로 둔다.**
    - 바꿀 때는 **명시적 diff** 로만 바꾸고 `version` 을 올리고 `changelog` 에
      이유·비용을 적는다. 폰트를 다시 구우면 `work/font-all/layout.sha256` 에
      각인이 남는다.
    - **디스크에 쓰기 전 `python3 scripts/verify_layout.py` 가 관문이다.**
      해시 일치, 못 박은 55칸 보존, 벡터 폰트 금지 자리(불변식 21) 비어 있음,
      번역문 음절 전부 존재, 전투 이름 글꼴 56칸 일치, 메뉴 고정폭 슬롯 전부
      들어감, 정본 아닌 배치를 읽는 스크립트 없음 — 아홉 가지를 본다.
25. **공용 계약을 바꾸기 전에 파급 목록을 먼저 적는다.** 「누가 이것을 읽는가」
    로는 모자란다. **「누가 이것에 제약을 거는가」** 를 적어야 한다. 배치에서
    음절 넷을 1바이트 구간 밖으로 옮겼다가 이름 입력 글자판(한 줄 5바이트
    고정)을 깨뜨렸고, 고쳐 놓고 다시 메뉴 그룹7 `재배열`(같은 5바이트 고정)을
    깨뜨렸다. 두 번 다 **「인코딩 실패 0건」으로 초록불이 떴다** — 인코딩은
    성공했고 넘친 것은 슬롯이었기 때문이다. 절차는 이렇다.
    1. **소비자를 전부 센다.** `grep` 으로 읽는 곳을 찾고, 각각이 거는 제약을
       한 줄로 적는다 (예: 「글자판 한 줄 5바이트 고정」, 「섹터 안에 들어갈 것」).
    2. **양이 아니라 형태가 바뀌는지 본다.** 「글자 수는 같다」가 아니라
       「바이트 수·정렬·인덱스 범위가 같은가」를 묻는다.
    3. **검사는 손으로 적지 말고 소비자 도구를 그대로 돌린다.** 슬롯 목록을
       손으로 옮겨 적었다가 그룹1 만 보고 그룹7 을 놓쳤다. `--dry-run` 이 있으면
       그것이 정본이다.
    4. **초록불의 의미를 확인한다.** 「실패 0건」이 무엇을 세는지 읽는다. 세지
       않는 것이 깨진다.
    5. 되돌릴 수 있게 한다 — 계약은 `version` 을 올리고 `changelog` 에 이유와
       비용을 적는다. 이번 것은 v2→v4→v6 로 두 번 되돌렸다.

## 원본 매체

```text
roms/Final Fantasy VIII (Japan, Asia) (Disc 1).cue
roms/Final Fantasy VIII (Japan, Asia) (Disc 1).bin
```

단일 `MODE2/2352` 데이터 트랙, 311,519섹터, CRC32 `2319B365`.

```bash
python3 scripts/psx_disc.py verify
python3 scripts/psx_disc.py toc
```

## 역공학 도구 선택

### 정적이냐 동적이냐는 매번 새로 판단한다

**요청마다 그 자리에서 고른다.** 앞 작업에서 쓴 방법을 관성으로 이어가지 않는다.
둘 다 필요하면 둘 다 쓰되, **먼저 답을 줄 쪽부터** 붙는다.

**정적 분석**(Ghidra, IDA, 디스어셈블 스크립트)이 맞는 때

- 주소·구조·상수를 **이미 알고** 그 의미를 읽어야 할 때
- 패치할 자리를 고르고, 원본과 대조하고, 부작용 범위를 볼 때
- 표·자료구조의 **전수 조사** — 파일에서 재면 되는 것을 실행으로 확인하지 않는다
- 실행 환경이 없거나, 재현이 비싸거나, 그 지점까지 몰고 가기 어려울 때

**동적 분석**(GDB 스텁, Lua, 런타임 스크립트)이 맞는 때

- **어디서** 죽는지, **무엇이** 그 자리를 망가뜨렸는지 모를 때 — PC 하나가
  정적 추적 몇 시간을 대신한다
- 실제로 어느 코드가 도는지가 쟁점일 때(오버레이·모듈이 여럿이라 정적으로는
  후보만 나온다)
- 값이 실행 중에 정해질 때(적재 주소, 포인터, 표의 실제 내용)
- 가설이 셋 이상 남았을 때. 추측을 늘리는 대신 **관측 한 번**으로 자른다

**고르는 기준 한 줄.** 「무엇이 어떻게 동작하는가」는 정적, **「지금 무슨 일이
벌어지고 있는가」는 동적**이다. 실기·에뮬레이터 왕복이 비싸게 느껴지기 시작하면
그것이 동적 도구를 붙일 신호다 — 가르는 실험은 판당 1비트지만 관측 한 번은
답 전체를 준다.

정적으로 좁히고 동적으로 확정하는 순서가 가장 자주 맞는다. 반대로 동적으로
자리를 잡은 뒤 그 주소만 정적으로 파는 것도 좋다. **한쪽만 고집하지 않는다.**

### 도구별 쓰임

- IDA Pro / `idalib-mcp`: 짧은 루틴, 바이트, xref, 함수 경계와 반복 질의의 주력.
- Ghidra MCP / `analyzeHeadless`: 긴 제어 흐름이나 포인터 전달을 MIPS 디컴파일로
  교차검증할 때 사용.
- DuckStation: 최종 육안 확인. **스크립팅 API 가 없어 자동화되지 않는다.**
- PCSX-Redux: 런타임 조사의 주력. **GDB 스텁(3333)과 웹 API(8080)를 함께
  연다** — `scripts/gdb_peek.py` 로 붙어 레지스터·메모리·감시점·단일 실행을
  모두 쓴다. mips 용 gdb 를 따로 깔 필요는 없다. 설정은
  `~/.config/pcsx-redux/pcsx.json` 에서 확인한다.
- 저장 파일과 runtime 표현이 다르면 정적 결과를 실행 증거로 승격하지 않는다.
- PS-X EXE는 직접 `idb_open` 하지 말고 `scripts/build_ida_db.py`로 올바른
  `TEXT`/entry가 있는 `.i64`를 만든 뒤 연다.

도구를 새로 붙일 때 기준은 "MCP 가 있는가"가 아니라 **밖에서 명령을 받는가**다.
CLI·HTTP·소켓이면 `Bash` 로 직접 몬다. 스크립팅 언어만 있으면 얇은 브리지를
한 겹 두른다. GUI 만 있으면 자동화 대상이 아니다. **CLI 나 HTTP 로 닿는 것에
MCP 를 새로 만들지 않는다.**

MCP 설정과 import 규칙은 `docs/reverse-engineering-mcp.md`를 따른다. `.mcp.json`은
클라이언트를 새로 시작해야 반영된다. GUI MCP는 해당 앱과 프로그램을 먼저 열어야
한다. headless idalib은 동일 IDB를 다른 프로세스와 동시에 열지 않는다.

## 확정된 핵심 주소

| 대상 | 값 |
|---|---|
| 부트 EXE | `SLPS_018.80`, entry `0x8001152C`, load `0x80010000`, text `0x190800` |
| 폰트 파일 | IMG TOC #130 (`sysfnt.tdw`), LBA 849. 원본 33,764B / 우리 것 29,724B (17섹터 배정 안) |
| 폰트 픽셀 오프셋 | CLUT 블록의 `blockSize` 가 정한다. 원본 1,036 / 우리 것 **1,164**(팔레트 36줄). **줄만 늘리고 길이를 안 고치면 픽셀 시작이 밀린다** |
| 폰트 적재기 | `sub_8002C358` — 압축을 풀지 않는다. 폭표 452B 복사 + `LoadImage` |
| 적재기 호출자 | `jal` 은 `sub_80011CA8` 하나(뱅크 0). 뱅크 1 은 오버레이가 `jalr` |
| 글리프 폭 | `sub_8002E3EC` — 니블 팩, 뱅크 비트 `0x400` |
| 문자열 측정 | `sub_8002E4A0` — **텍스트 인코딩 정본** |
| 글리프 그리기 | CLUT 상수 4곳 — `0x8002E834` `0x8002E9EC` `0x8002EC58` `0x8002F090`. **짝수 `0x3812` / 홀수 `0x3852` 가 쌍이다.** 다섯째 경로 `0x80033118` 은 패리티를 안 더한다 |
| TIM 로더 | `sub_80035EC4` / `LoadImage` `sub_80048DA0` |
| LZSS 해제기 | `0x800393A4`~`0x800394F8`. **선두 u32 는 압축 길이**, 거리 1~4095 |
| 폭 테이블 RAM | 뱅크0 `0x800821F8` / 뱅크1 `0x800823BC` / 다음 구조체 `0x80082580` |
| VRAM 폰트 슬롯 | 뱅크0 **`(960,256)`**, 뱅크1 **`(832,256)`** — 런타임 확인 |
| VRAM CLUT | 원본 `(288,224)` 16벌. 우리 것 **`(960,472)` 36줄**(테마 32 + 그림자 4). 적재기가 x·y 를 상수로 덮어쓴다(`0x8002c408`/`0x8002c410`) |
| IMG TOC 적재 | LBA 826, 1,072바이트 = 134엔트리 → RAM `0x80099400` |
| CD 읽기 | `sub_800385E4` 요청 세터(목적지 `dword_8008924C`에 씀) / `sub_8003924C` 상태 머신 펌프 — `byte_80089231`(상태)로 표 `0x80055110`(4바이트×N)를 `jalr` 디스패치. 상태 7~10 = `sub_80038EC4`→`sub_80038F18`→`sub_80038FC4`→`sub_8003909C`(마지막이 LZSS 해제 펌프). **배경음악/효과음 스트리밍도 이 경로를 공유** — 노이즈 주의(`docs/lessons.md` 13-1) |
| 아카이브 해석기 | `sub_80035C84(sync, archive_idx, sub_idx, dest)` — `LBA = dword_80099420[2*archive_idx]`(archive_idx = TOC − 4). `sub_idx<0`이면 아카이브 전체, 아니면 서브엔트리 디렉터리로 섹터 오프셋 계산 |
| 아카이브 로더 진입점 | `sub_80035F48(sync,archive_idx,sub_idx,dest,cb,cbarg)` — 작업을 링버퍼 `dword_80083FD0`(8칸×20B, 카운터 `dword_80083FA8`)에 적어 두고 `sub_80035C84` 호출. 이걸 부르는 4자매: `sub_80036024`(표 `0x80054340` 기반, 메뉴 오버레이) / `sub_80036078`·`sub_800360E4`(archive 18 고정 = mngrp.bin) / **`sub_800360AC(archive_idx, dest)`(호출자가 번호를 직접 넘기는 유일한 변형)**. `sub_800360AC` 자신의 정적 호출자는 **못 찾았다** — 부트 EXE, 상주 메뉴 모듈(`0x80090000`~`0x800E0000`), RAM `0x80090000`~`0x80108000` 전체를 `jal` 바이트열로 훑어도 안 나온다(`jalr` 간접 호출이거나 일시 상주 코드로 추정) |
| 전투 모듈 적재 | `0x8009BD30`(오버레이 슬롯)은 **LZSS 압축**으로 실린다 — `sub_800360AC` 경로로 추정(실행 중 감시점으로 목적지 확인, 호출자는 미확정). 압축 해제는 `0x800393A4`~`0x800394F8` |
| 전투 모듈 내부 스프라이트 시스템 | `sub_8009EA1C`(모듈 오프셋 `0x2CEC`) 레코드 파서(커서 스트림, TIM과 유사하나 순차) / `sub_8009EAC8`(오프셋 `0x2D98`) 레코드 표 순회 CD 스트리밍 루프. 오프셋 표 조회 `a0 = a3 + *(a3+idx*4)`(`0x800a9c30`)로 시작 커서를 구한다 — `a3` 기준 주소는 미추적. 전투 HP 이름 글꼴 출처 후보(`docs/roadmap.md` 항목 6 인계 메모 참고) |
| 오버레이 목적지 표 | `0x80054340` — `(u32 dest, u32 이름포인터)` × 23 |
| 서브엔트리 디렉터리 | `0x800543F8` — `(u32 오프셋+플래그, u32 크기)`, `>> 11` 로 섹터 |
| 메뉴 메시지 | `mngrp.bin`(TOC#22) 서브 1, LBA 98,036, 10,240B → RAM `0x801E1000`. **그룹 오프셋은 4의 배수여야 한다** |
| 필드 색인 | `field.bin`(TOC#2) 해제본 `+0x28668`, 1,003엔트리 `[u32 LBA][u32 크기]` |
| 필드 텍스트 | 각 필드 DAT 의 섹션 포인터 `[8]` = MSD, `[u32 오프셋 배열]` |

### 4중 인터리브 패치 (채택)

글리프 칸을 882 → **1,512** 로 올린다. 바뀐 워드 41개(그림자 제외).

| 자리 | 전 → 후 |
|---|---|
| `0x8002C420` | `slti v0,v0,0x11` → `0x25` — CLUT 높이 36 허용 |
| `0x8002C470` | `addiu t0,a2,0x1c0` → `0x370` — 폭표 884바이트 복사 |
| `0x8002C3B8` | `bne t0,zero,0x8002c444` → `0x8002c51c` — 필드 한자표 차단 |
| `0x8002C408` / `0x8002C410` | `0x120`/`0xe0` → `0x3c0`/`0x1d8` — CLUT `(960,472)` |
| `0x23bc` 7곳 | → `0x23b1` — 통합 폭표의 뱅크1 시작 |
| `0x3812` 5곳 / `0x3852` 4곳 | → `0x763c` / `0x767c` — **짝을 같이 고친다** |
| 그리기 4곳 | tpage 분기 6슬롯을 CLUT 보정으로 교체 |
| 오버레이 TOC #4·#26 | 자기 팔레트 상수 쌍을 갖는다 — `patch_overlay_clut.py` |

칸이 1,764 가 아니라 1,512 인 것은 팔레트를 **뱅크0 텍스처 사각형 안쪽**
`(960,472)` 로 옮기며 21행을 18행으로 줄였기 때문이다. 원본 CLUT 아래 16줄
`(288,240)` 은 남의 CLUT 가 덮는 자리였다.

그리기 패치는 **적재 지연 슬롯을 지킨다** — `lhu` 다음 명령이 그 값을 읽으면
실기에서만 틀린다.

**뱅크1 의 VRAM 은 쓰지 않는다.** 동영상이 `(832,256)` 을 지운다(실측: 뱅크0
100% / 뱅크1 32.5%). 텍스처는 뱅크0 것 하나만 쓰고 뱅크1 에서는 인코딩 공간과
폭 테이블만 빌린다.

## 기여 표기

**작성 도구를 저자·공동저자·출처로 기입하지 않는다.** 커밋 메시지, PR 본문,
문서 어디에도 도구 이름이 들어간 트레일러를 넣지 않는다.

## 기본 검증

```bash
python3 scripts/psx_disc.py verify
python3 scripts/psx_disc.py toc
python3 scripts/build_ida_db.py work/disc1/SLPS_018.80 --force
```

패치를 만들었으면 아래를 **전부** 통과해야 실기에 올린다.

```bash
# 실행 중인 것이 우리 빌드인가 + 폰트가 VRAM 에 온전한가
python3 scripts/verify_font_4plane.py

# 고쳐 쓴 섹터의 EDC/ECC
python3 scripts/psx_sector.py verify
```

그리고 손으로 확인할 것 셋 — **주입 코드의 적재 지연 슬롯**, **다시 짠
자료구조의 오프셋 정렬**, **원본과 전체 대조해 바꾼 곳이 의도한 데뿐인가**.
