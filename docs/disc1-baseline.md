# Disc 1 수정 전 역공학 기준선

검토일: 2026-07-27
대상: `Final Fantasy VIII (Japan, Asia) (Disc 1)`

이 문서는 한국어 데이터를 쓰기 전에 고정한 구조 기준선이다. 여기서 "완전"은
Disc 1의 저장 단위가 빠짐없이 분모에 들어가고, 각 단위가 알려진 종류 중
하나로 분류됐다는 뜻이다. 게임의 모든 실행 코드나 모든 텍스트를 해독했다는
뜻이 아니다.

현재 새 바이너리 수정은 시작하지 않았다.

## 원본 기준

기본 원본 위치:

```text
roms/Final Fantasy VIII (Japan, Asia) (Disc 1).cue
roms/Final Fantasy VIII (Japan, Asia) (Disc 1).bin
```

CUE는 단일 트랙이다. CDDA 트랙은 없다.

```text
FILE "Final Fantasy VIII (Japan, Asia) (Disc 1).bin" BINARY
  TRACK 01 MODE2/2352
    INDEX 01 00:00:00
```

| 식별값 | 값 |
|---|---|
| 크기 | 732,692,688바이트 |
| raw sector | 311,519 |
| CRC32 | `2319B365` |
| MD5 | `cd2a9d4a92c8cea33b434fd41bbf002c` |
| SHA-256 | `6e26677aca74e364fa90f287d0755fb3777d20d841f4d4a0917801eaa5da42eb` |

표본 검사한 sector는 모두 Mode 2 Form 1이고 sync가 정상이며 subheader가
복제돼 있다. 원본은 읽기 전용으로 취급하며 `config/original-media.json`과
`scripts/psx_disc.py verify`가 위치와 식별값을 검증한다.

## 2단 컨테이너 구조

FF8은 사이버 포뮬러처럼 개별 자산을 ISO 루트에 노출하지 않는다. 바깥
ISO9660에는 파일이 **정확히 3개**뿐이고, 게임 데이터 전부가 단일 컨테이너
안에 들어 있다.

| LBA | 크기 | 파일 |
|---:|---:|---|
| 23 | 68 | `/SYSTEM.CNF` |
| 24 | 1,642,496 | `/SLPS_018.80` |
| 826 | 635,992,064 | `/FF8DISC1.IMG` |

PVD의 system identifier와 application identifier는 모두 `PLAYSTATION`이고
volume identifier는 비어 있다. 논리 블록 수는 311,519 × 2048이다.

```text
BOOT = cdrom:\SLPS_018.80;1
TCB = 4
EVENT = 16
STACK = 80200000
```

**PC판의 `menu.fs` / `Data\menu\...` 경로는 이 디스크에 존재하지 않는다.**
PC판 자료를 참고할 때 경로 이름으로 PSX 자산을 찾으려 하면 안 된다. PSX에서는
아래 숫자 인덱스로 접근한다.

## 실행 파일

부트 파일 `SLPS_018.80`의 PS-X EXE 경계:

| 항목 | 값 |
|---|---:|
| entry | `0x8001152C` |
| load address | `0x80010000` |
| text size | `0x190800` |
| file payload | `+0x800` |
| data / bss / stack | `0` |
| region 문자열 | `Sony Computer Entertainment Inc. for Japan area` |

파일 전체가 header + text이며 별도 data section이 없다. IDA에서 함수 1,472개가
인식되고 그중 417개에 PsyQ 런타임 signature가 적용된다.

## IMG TOC — 파일 분모

`FF8DISC1.IMG`의 **첫 섹터가 곧 목차**다. 2048바이트를
`(u32 절대 LBA, u32 바이트 크기)` 쌍 256개로 읽으며, 그중 **134개가 유효**하다.
파일명 문자열은 없다.

배치 규칙은 `다음 엔트리 LBA = 현재 LBA + ceil(size / 2048)`이며 134개 중
125개가 이를 만족한다. 규칙이 끊기는 8곳이 그룹 경계다.

LBA 범위는 827..99,305이고 모두 디스크 범위 안에 있다. **TOC가 담는 값은 IMG
내부 상대 오프셋이 아니라 디스크 절대 LBA다.** 자산 크기를 바꾸면 뒤 파일이
밀리고 TOC 전체를 다시 써야 하므로, 기본 전략은 크기를 유지하는 in-place
교체다.

### 이 TOC는 디스크 전체의 분모가 아니다

TOC 134개가 덮는 섹터는 **10,161개(약 20MB)** 뿐이다. 디스크는 311,519섹터
(약 732MB)이므로 **300,532섹터(약 615MB)가 이 TOC 밖에 있다.**

밖의 구역을 표본 조사한 결과는 다음과 같다.

| 구역 | 내용 |
|---|---|
| LBA 99,306 부근 | u32 오프셋 테이블을 가진 추가 아카이브 |
| LBA 약 180,000 이상 | 매직 `SMR` 로 시작하는 **FMV 동영상 스트림** |
| LBA 311,500 부근 | Mode 2 **Form 2** 섹터 (스트리밍 미디어 꼬리) |

표본 1,000섹터 중 Form 2는 0개였고 대부분 Form 1이다. FF8의 FMV가 Form 1
`SMR` 컨테이너를 쓴다는 뜻이다.

**따라서 "파일 분모가 닫혔다"고 말할 수 있는 범위는 TOC가 덮는 20MB뿐이다.**
폰트·코드·필드·사운드는 이 안에 있으므로 한국어화 1단계에는 충분하지만,
디스크 전체를 재구성하려면 TOC 밖 구역의 인덱스를 따로 찾아야 한다.

### 종류별 분류

`scripts/psx_disc.py toc`가 선두 바이트로 판정한 결과다.

| 종류 | 개수 | 판정 근거 |
|---|---:|---|
| `field-archive` | 98 | 선두가 `02 00 00 00` + `14 00 00 00` 인 미니 아카이브 |
| `unknown` | 14 | 아직 판정 근거가 없는 엔트리 |
| `mips-overlay` | 12 | 선두가 `addiu $sp, $sp, -N` 프롤로그 |
| `lzss` | 4 | 선두 u32 == 전체 크기 − 4 |
| `akao-sound` | 4 | 매직 `AKAO` |
| `font` | 1 | `[u32 표 오프셋][u32 TIM 오프셋]` 구조 |
| `tim` | 1 | 매직 `0x00000010` |

합계 134. 판정 근거가 없는 14개는 **추측으로 채우지 않고 `unknown`으로 둔다.**

### 주요 엔트리

이름이 붙은 것은 **부트 EXE 안의 이름 문자열 표에서 직접 읽은 값**이다
(`0x80054340` 의 두 번째 u32 가 이름 포인터다). 추정이 아니다.

| # | LBA | 크기 | 이름 / 내용 |
|---:|---:|---:|---|
| 0 | 827 | 1,176 | MIPS 코드 |
| 1 | 828 | 4,146 | LZSS → 6,960B, Shift-JIS 텍스트 포함 |
| **2** | **33,249** | **102,500** | **`field.bin`** — LZSS → 191,824B, **필드 파일 색인** |
| 3 | 33,300 | 1,728 | `wm2field.tbl` (72 × 24 = 1,728B, 크기가 정확히 일치) |
| 4 | 97,859 | 42,800 | `menumain.ovl` → RAM `0x801F0000` |
| 5..21 | 97,880.. | — | `menucfg` … `menutest` 오버레이 17개 (아래 표) |
| **22** | **98,035** | **2,168,832** | **`mngrp.bin`** — 메뉴 그룹 아카이브 |
| 23 | 99,094 | 2,812 | `init.out` |
| 24 | 971 | 683,003 | LZSS → 1,091,704B, 트리플 트라이어드 모듈 |
| 25 | 99,096 | 80,376 | ASCII 빌드 타임스탬프 `Jan 10 1999` |
| 26 | 99,136 | 346,560 | RAM 포인터 배열 (`0x8009BDB8` 계열) |
| 27 | 1,305 | 118,826 | LZSS → 198,584B, 월드맵 모듈 |
| 28..30, 133 | 885.. | — | `AKAO` 사운드 시퀀스 |
| **31..128** | **87,862..** | — | **`AKAO` 음악 파일 98개** (필드 데이터가 아니다) |
| 129 | 831 | 35,976 | u32 오프셋 배열 아카이브 |
| **130** | **849** | **33,764** | **12×12 폰트** (`docs/font-analysis.md`) |
| 131 | 866 | 33,312 | 4bpp 256×256 TIM, 베이크드 UI 그래픽 |

> **정정.** 이전 판은 #31..#128 을 "필드 아카이브 98개" 로 적었다. 실제로는
> **음악 파일**이다. 각 파일의 서브엔트리 0 이 `AKAO` 이고, 이 안에서 메시지
> 테이블을 전수 탐색한 결과 후보가 0개였다. 필드 데이터는 `field.bin`(#2)이
> 색인하는 별도 구역에 있다.

## 모듈 적재 경로

부트 EXE 안에서 디스크 → RAM 경로를 전수 추적했다. 세 계층이다.

```text
sub_80011E10                 IMG TOC 를 RAM 0x80099400 에 적재
                             LBA 826, 1,072바이트 = 정확히 134엔트리
  └─ 각 로더 스텁            0x80099400 + 8N 에서 (u32 LBA, u32 size) 를 읽는다
       └─ sub_800386B0       → sub_800385E4  읽기 요청 세터
            └─ sub_8003924C  상태 머신을 idle 까지 펌프
```

**부트스트랩 상수 `LBA 826 / 1,072바이트` 가 유효 엔트리 134개를 코드로
확증한다.** 정적 파싱으로 센 134와 독립적으로 일치한다.

### 기준 주소가 둘이다

코드에 따라 TOC 기준이 다르므로 인덱스를 섞으면 안 된다.

| 기준 | 값 | 쓰는 곳 |
|---|---|---|
| TOC#0 기준 | `0x80099400` | 하드코딩 로더 스텁 |
| TOC#4 기준 | `0x80099420` | 오버레이·아카이브 로더 (`아카이브 번호 = TOC − 4`) |
| TOC#31 기준 | `0x800994F8` | `sub_80037E34` |

### 하드코딩 적재

`scripts/trace_module_loader.py` 가 뽑은 표다.

| 호출 지점 | 출처 | 목적지 |
|---|---|---|
| `0x80011E30` | LBA 826, 1,072B | `0x80099400` (TOC 자신) |
| `0x80011C80` | TOC#0 | `0x8009A000` |
| `0x80011AA8` | TOC#2 | `0x8009A000` |
| `0x80011B78` | TOC#3 | `0x80099940` |
| `0x80011B28` | TOC#27 | `0x8009A000` |
| `0x80011BC8` | TOC#28 | `0x80066188` |
| `0x80011BFC` `0x80011C30` | TOC#29, #30 | `0x801B0000` |
| `0x80011CC4` | **TOC#130 (폰트)** | **`0x801B0000`** |
| `0x80011D70` | TOC#131 | `0x801B0000` |
| `0x80011DA4` `0x80011DE0` | TOC#133, #132 | `0x80090000` |

### 오버레이 목적지 표 — `0x80054340`

`sub_80036024` 가 `*(u32*)(0x80054340 + 8 * 아카이브)` 로 목적지를 읽는다.
두 번째 u32 는 이름 문자열 포인터다.

| TOC | 이름 | RAM |
|---:|---|---|
| 4 | `menumain.ovl` | `0x801F0000` |
| 8 · 12 · 16 · 18 | `menuabl` · `menuitem` · `menusav` · `menututo` | `0x801E4000` |
| 5·6·7·9·10·11·13·14·15·17·19·20·21 | `menucfg` `menupty` `menusts` `menushop` `menuext` `menutphs` `menumgc` `menugf` `menujnc2` `menucrd` `menutmag` `menutips` `menutest` | `0x801E6000` |

**이 표가 `wrap_psx_overlay.py --solve` 의 역산 결과(#4 = `0x801F0000`,
#12 = `0x801E4000`)를 독립적으로 확증한다.** 휴리스틱과 정적 표가 일치한다.

### 서브엔트리 디렉터리 — `0x800543F8`

`sub_80035C84` 가 쓰는 정적 표다. 형식은 `(u32 오프셋|플래그, u32 크기)` 이며
빈 슬롯은 `0xFFFFFFFF` 다.

```text
실제 LBA = TOC[4 + 아카이브].LBA + ((오프셋 & ~1) >> 11)
읽기 경로 = (오프셋 & 1) ? sub_80035C44 : sub_80035C64
```

**오프셋을 `>> 11` 하므로 서브엔트리는 섹터 정렬이다.** 한국어 데이터를 끼워
넣을 때의 크기 제약이 여기서 나온다.

실측된 주요 서브엔트리(아카이브 18 = TOC#22 = `mngrp.bin`, LBA 98,035):

| 서브 | 섹터 오프셋 | 크기 | 실제 LBA | 내용 |
|---:|---:|---:|---:|---|
| 0 | 0 | 2,048 | 98,035 | — |
| **1** | **1** | **10,240** | **98,036** | **메뉴 메시지 아카이브 → RAM `0x801E1000`** |
| 2 | 6 | 57,344 | 98,041 | — |
| 9 | 36 | 26,624 | 98,071 | TIM (→ `0x801CE000`) |
| 10 | 49 | 26,624 | 98,084 | TIM (→ `0x801D6000`) |

### 필드 데이터 색인 — `field.bin`

필드는 IMG TOC 가 아니라 `field.bin`(TOC#2)이 색인한다. LZSS 해제 후
오프셋 `0x28668` 부터 **8,024바이트 = 엔트리 1,003개**이고, 형식은 IMG TOC 와
같은 `[u32 절대 LBA][u32 크기]` 다. LBA 범위는 33,301..87,815 이며 **정렬돼
있지 않다.** 각 필드의 `*.DAT` 안 MSD 섹션이 대사를 담는다. 자세한 내용은
`docs/font-analysis.md` 의 "필드 메시지 — MSD 섹션" 절을 본다.

### Ghidra 교차검증

위 판정의 근거 함수 네 개를 Ghidra 로 독립 디컴파일해 대조했다.
`FUN_80035c84` 가 `&DAT_80099420 + param_2 * 8` 과 `&DAT_800543f8 + param_3 * 8`
의 `& 1` / `>> 0xb` 를, `FUN_80036024` 가 `&DAT_80054340 + param_1 * 8` 을,
`FUN_80037e34` 가 `&DAT_800994f8 + param_2 * 8` 을, `FUN_800385e4` 가
`param_3 + 0x7ff >> 0xb` 를 그대로 냈다. IDA 디스어셈블과 일치한다.

> **Hex-Rays 를 이 경로에서 그대로 믿으면 안 된다.** `sub_80011CA8` 계열에서
> IDA 디컴파일이 `lw $a0` / `lw $a1` 을 통째로 누락해 `(0, 0, dest, 0)` 으로
> 보여 준다. `sub_8003924C` 도 `nullsub_5(); return 0;` 으로 잘못 풀어
> 폴링 루프가 no-op 처럼 보인다. 실제로는 점프 테이블
> `off_80055110[byte_80089231]` 을 호출하고 갱신된 상태값을 반환한다.
> **이 경로는 디스어셈블이 정본이다.**

## 압축

압축된 엔트리는 `[u32 원본 크기][LZSS 스트림]` 형식이다. LZSS는 Okumura 계열,
4096바이트 링버퍼, 초기 위치 `0xFEE`, 제어 바이트 8비트(LSB 우선), 참조는
2바이트로 `offset = lo | ((hi & 0xF0) << 4)`, `length = (hi & 0x0F) + 3`이다.

`scripts/psx_disc.py extract`가 해제까지 수행한다. 4개 엔트리에서 1.60~1.87배로
해제되며, #27은 다음과 같은 깨끗한 경로 문자열을 낸다.

```text
\DAT\TEXL.OBJ;1   \DAT\WMY.OBJ;1    \DAT\WMX.OBJ;1
\DAT\RAIL.OBJ;1   \DAT\WMSET.OBJ;1  \DAT\MUSIC0..5.OBJ;1
x:\JPPC\WORLD     x:\jppc\world\esk
```

**TOC 최상위에서 LZSS인 엔트리는 4개뿐이다.** 나머지는 비압축이거나 내부
서브아카이브 단위로 압축된다. 후자는 아직 조사하지 않았다.

## 그래픽 자산

TIM은 표준 형식이며 게임의 로더는 `sub_80035EC4`다. 다음을 확인했다.

```c
if (*(BYTE *)file == 0x10) {              // TIM 매직
    clut = file + 8;
    if (*(BYTE *)(file + 4) & 8)          // flag & 8 → CLUT 있음
        image = file + *(DWORD *)(file + 8) + 8;
    else { clut = 0; image = file + 8; }
    if (clut) { LoadImage(clut + 4, clut + 12); DrawSync(0); }
    return LoadImage(image + 4, image + 12);
}
```

즉 **RECT(`blk + 4`)와 픽셀 포인터(`blk + 12`)만 사용하고 `blockSize`는 쓰지
않는다.** 파일에 기록된 `blockSize`가 실제 데이터 길이와 다른 경우가 있으므로
RECT를 정본으로 삼는다.

번역 대상이 확인된 베이크드 그래픽:

- 엔트리 #24(트리플 트라이어드): `You Win!` / `You Lose...` / `Draw` /
  `SAME!` / `PLUS!` / `COMBO!` 와 카드 숫자
- 엔트리 #131: LBA 866, 33,312B. `[magic 0x10][flag 0x08][CLUT 524B:
  16×16 팔레트][이미지 32,780B: VRAM(896,256) 256×256px 4bpp]`. **CLUT을
  제대로 입혀 디코드해 용도 확정**(2026-08-06, `roms/*.bin`에서 직접 추출).
  4bpp를 팔레트 없이 그레이스케일로만 보면 절반 가까운 글자가 안 보여
  "글자가 몇 개 없다"는 오판을 낳는다 — **팔레트를 반드시 입혀서 판정한다.**
  실제 내용: 숫자 3벌(이탤릭형 / TRIPLE·DOUBLE형 / `Miss`형 — 이 중
  `Miss`형이 실전 전투 HP 숫자와 스타일이 가장 가깝다), 캐릭터 이름 3명
  (`スコール`/`ゼル`/`リノア`, 키스티스 없음), `NAME`/`HP`/`TIME`/`PAUSE`/
  `ITEM`/`COMMAND`/`ATTACK`/`DEFENSE`/`ELEMENTAL` 등 라벨 조각, 연속검
  판정(`Hit!`/`Trigger!!`/`Perfect!!`), 바이브레이션 설정, 이름 입력
  키보드, 트리플 트라이어드 UI, 아이템·상태이상 아이콘.
  - **보정 (2026-08-08)**: 「이 시트에 전투 이름이 없다」는 것은 **디스크의
    정적 내용**에 대해서만 맞다. 실행 중에는 게임이 **같은 자리
    (VRAM halfword x≈900..958, y≈352..380)에 현재 파티의 이름을 통짜 그림으로
    조립해 덮어쓴다.** 조립에 쓰는 글꼴은 뱅크0 도 이 시트도 아닌 **제3의
    일본어 가나 시트**(런타임 `0x801b8e00`)다. 여기 적힌 3명
    (`スコール`/`ゼル`/`リノア`)은 디스크에 미리 그려진 기본 이름이고, 실전에서
    보이는 것은 그 위에 덮인 조립본이다. 자세한 것은 `docs/roadmap.md` 항목 6
- 엔트리 #22: 8bpp 256×192 / 320×216 이미지 38장

개별 그래픽의 편집 게이트는 실제 화면 소비 경로(Texpage/UV/CLUT, VRAM을 마지막
으로 채운 전송)를 확인하기 전까지 닫혀 있다. 구조가 파악됐다는 이유만으로
일괄 수정하지 않는다.

## 아직 닫히지 않은 항목

1. **`unknown` 엔트리** — 이름 표(`0x80054340`)가 덮는 것은 아카이브 0..19
   (TOC #4..#23)뿐이다. 그 밖의 미분류 엔트리는 판정 근거가 없다.
2. **TOC #22 `mngrp.bin` 의 나머지 서브엔트리** — 서브 1(메시지)과 9·10(TIM)
   외에는 내용을 열지 않았다.
3. **필드 파일 목록 1,003개의 종류 대응** — MIM/DAT/LZK 구분을 목록만으로는
   못 한다. 현재는 해제 후 구조로 DAT 를 걸러낸다.
4. **TOC 밖 잔여 구역** — `field.bin` 이 색인하는 33,301..87,815 를 빼도
   LBA 약 100,000 이상(FMV 포함)은 여전히 색인을 찾지 못했다.
5. **런타임 검증** — 실행기에서 VRAM·RAM을 확인하지 않았다. 현재 판정은
   전부 정적 분석과 두 디컴파일러의 교차검증에 근거한다.
6. **`kernel.bin`(TOC#129)의 런타임 RAM 위치** — 제어 코드 `0x0C`/`0x0D`가
   `RAM 0x80077B20` 기준 오프셋으로 문자열을 가리키는 것을 확인했으나
   (`docs/external-sources.md` E절), 이 주소가 실제로 kernel.bin의 적재
   목적지인지 로더 호출부를 아직 못 찾았다. **추정**으로만 남긴다.

> 이전 판의 "텍스트 인코딩 테이블 미확정" 과 "필드 아카이브 내부 구조" 는
> 해소됐다. 각각 `docs/font-analysis.md` 의 인코딩 절과 MSD 절을 본다.

## 재현 명령

```bash
python3 scripts/psx_disc.py verify
python3 scripts/psx_disc.py iso
python3 scripts/psx_disc.py toc
python3 scripts/psx_disc.py extract --index 1 2 24 27
python3 scripts/psx_disc.py report --output work/analysis/disc1-structure.json
python3 scripts/build_ida_db.py work/disc1/SLPS_018.80 --force

# 모듈 로더: TOC 인덱스 <-> RAM 목적지, 오버레이 이름, 서브엔트리 디렉터리
python3 scripts/trace_module_loader.py work/disc1/SLPS_018.80 --archives \
    --json work/analysis/loader-boot.json
```

`work/`의 추출물·DB·이미지는 커밋하지 않는다. 저장소에는 생성기, 주소·수량·
해시와 판정만 남긴다.
